export default function LoginLayout({ children }) {
    return (
        <html>
        <body>
        <main>
            <h1>Авторизация</h1>
            {children}
        </main>
        </body>
        </html>
    );
}
